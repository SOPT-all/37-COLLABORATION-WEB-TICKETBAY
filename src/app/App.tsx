function App() {
  return (
    <div style={{ padding: "20px" }}>
      <h1>티켓베이 프로젝트</h1>

      {/*
        ⚠️ 아이콘 관련 안내
        - 아이콘은 @assets/icons 배럴 파일에서 개별 export 형태로 관리합니다.
        - 실제 아이콘이 추가되면 아래처럼 import하여 UI에 사용할 수 있습니다:

          import { TimeIcon } from "@assets/icons";

          <TimeIcon width={24} height={24} />

        - 현재는 테스트용 SVG(test.svg)가 제거되었기 때문에 표시되는 아이콘은 없습니다.
      */}
    </div>
  );
}

export default App;
